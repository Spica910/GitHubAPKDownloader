<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.GitHubAPKDownloader"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SetupActivity"
            android:exported="false"
            android:label="Setup" />

        <activity
            android:name=".RepositoryListActivity"
            android:exported="false"
            android:label="Repositories" />

        <activity
            android:name=".ReleasesActivity"
            android:exported="false"
            android:label="Releases" />

        <activity
            android:name=".FileTreeActivity"
            android:exported="false"
            android:label="Files" />

        <activity
            android:name=".FileBrowserActivity"
            android:exported="false"
            android:label="Select File" />

        <activity
            android:name=".AiBuildActivity"
            android:exported="false"
            android:label="AI Build" />

        <activity
            android:name=".CliSetupActivity"
            android:exported="false"
            android:label="CLI Setup" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
